## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
## Guide to the repo
- `src/app/layout.txt` : page title (metadata), definition of structure - Navbar, Footer
- `src/app` : different pages for routing